# Placeholder for the app icon.
#
# Replace with a multi-resolution `AppIcon.icns` before v1 ships. The
# .app launches fine without one (Finder uses a generic icon), so
# this isn't gating the release pipeline — but the DMG looks
# noticeably unfinished without it.
#
# Build with `iconutil -c icns AppIcon.iconset/` from a directory
# containing 16x16, 32x32, ..., 1024x1024 PNGs (see Apple's
# `iconutil` docs for the canonical naming scheme).
