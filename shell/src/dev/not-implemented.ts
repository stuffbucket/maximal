/**
 * Scaffolding marker for the single-window redesign (shell side).
 * See src/lib/dev/not-implemented.ts for the rationale. Delete once filled in.
 */
export function notImplemented(name: string, _refs?: Record<string, unknown>): never {
  throw new Error(`TODO(single-window): ${name} is scaffolded but not implemented`);
}
