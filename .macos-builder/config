app_path = shell/src-tauri/target/release/bundle/macos/Maximal.app
volname = Maximal
bundle_id = co.stuffbucket.maximal
entitlements = bun-runtime
artifact = dmg

# The builder signs this nested Bun sidecar after the isolated producer exits.
sign_nested = Contents/MacOS/maximal:bun-runtime
